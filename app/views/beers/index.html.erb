<!DOCTYPE html>
<html lang="en">

<h1>This is the BEERS::INDEX view</h1>


<% @beers.each do |b| %>
  <%= link_to b.name, beer_path(b) %>
  <br>
  | Style: <%= b.style %>
  | Manufacturer: <%= b.manufacturer %>
  <br>
  <br>
<% end %>
</html>

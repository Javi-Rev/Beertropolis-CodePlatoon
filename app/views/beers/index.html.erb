<!DOCTYPE html>
<html lang="en">

<h2>The Beer List</h2>


<% @beers.each do |b| %>
  <%= link_to b.name, beer_path(b) %>
  <br>
  | Style: <%= b.style %>
  | Manufacturer: <%= b.manufacturer %>
  <br>
  <br>
<% end %>
</html>

<!DOCTYPE html>
<html lang='en'>


<%= form_for @beer do |f| %>

  <% if @beer.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@beer.errors.count, "error") %>
prohibited
        this beer from being saved:
      </h2>
      <ul>
        <%= @beer.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <p>
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </p>

  <p>
    <%= f.label :style %><br>
    <%= f.text_field :style %>
  </p>

  <p>
    <%= f.label :manufacturer %><br>
    <%= f.text_field :manufacturer %>
  </p>

  <p>
    <%= f.submit %>
  </p>

<% end %>
 </html> 
